# 학습 로그

## 메타인지 하기

### 1. 현재 TypeScript 경험

- TS를 써본 적이 있는가? 있다면 어느 정도?

### 1. 기초 (Beginner)

- [x] 기본 타입(string, number, boolean, null, undefined)의 차이를 설명할 수 있다.
- [x] interface와 type alias의 차이점과 각각의 사용 사례를 안다.
- [x] 함수의 매개변수, 반환값, 선택적 매개변수(?)에 타입을 지정할 수 있다.
- [x] 배열(number[])과 튜플([number, string])의 구조적 차이를 이해한다.
- [x] 왜 any 사용을 지양해야 하는지, unknown과의 차이가 무엇인지 안다.

### 2. 중급 (Intermediate)

- [x] 제네릭(Generics)을 사용해 재사용 가능한 함수나 클래스를 설계할 수 있다.
- [x] 유니온(|)과 인터섹션(&) 타입을 활용해 데이터를 조합할 수 있다.
- [ ] 타입 추론의 원리를 이해하고, 'as' 단언(Assertion)의 위험성을 설명할 수 있다.
- [ ] 타입 가드(typeof, instanceof, in, 사용자 정의 가드)로 타입을 좁힐 수 있다.
- [x] Utility Types(Partial, Pick, Omit, Record 등)를 적재적소에 사용한다.
- [ ] Enum의 한계를 이해하고 as const나 Union type으로 대체할 수 있다.

### 3. 고급 (Advanced)

- [ ] 조건부 타입(Conditional Types: T extends U ? X : Y)을 구현할 수 있다.
- [ ] infer 키워드를 사용하여 조건부 타입 내에서 특정 타입을 추출할 수 있다.
- [x] 매핑된 타입(Mapped Types)과 keyof를 조합해 기존 타입을 변형할 수 있다.
- [x] 템플릿 리터럴 타입을 활용해 정교한 문자열 패턴 타입을 정의할 수 있다.
- [ ] 공변성(Covariance)과 반공변성(Contravariance)의 개념을 이해한다.
- [ ] Module Augmentation으로 외부 라이브러리의 타입 정의를 확장할 수 있다.

### 4. 실전 및 환경 (Ecosystem)

- [ ] tsconfig.json의 주요 옵션(strict, paths, target 등)의 역할을 안다.
- [ ] 타입 정의가 없는 라이브러리에 대해 직접 .d.ts 파일을 작성할 수 있다.
- [ ] React 환경에서 Props, Event, useRef 등에 정확한 타입을 입힐 수 있다.
- [x] 컴포넌트 설계 시 제네릭을 활용하여 Props의 유연성을 확보할 수 있다.

### 2. 기존에 새로운 개념을 학습할 때 어떻게 했나요? (수업시간에 v1 떠올렸던 것 첨부)

> 1. 책/강의 등 떠오른 지식 공유자를 선택해서 이론 학습을 한다.
> 2. 그러다보면 따분히 앉아있는 시간을 버틸 수 없어진다...
> 3. TS, tailwind처럼 직접 적용해 볼 수 있는 것들은 직접 시도해보면서 감을 잡아간다.
> 4. 그럴 수 없거나 막히는 부분은 대체로 AI에게 물어본다.
> 5. AI에게는 주로 내가 이해하고 있는 개념이 맞는지를 물어본다.
> 6. AI가 답변을 제대로 못하면 다시 책을 찾아보거나 공식 문서를 찾아본다. 블로그보다는 공식문서를 보게 된 것 같다. 강의를 듣는 경우 강의자에게 질문하기도 한다.

## 학습 로그 #1

**시간**: MM/DD HH:MM ~ HH:MM (약 \_\_분)
**학습 범위**: (예: typescript-exercises #1~#3)

### 1. 첫 학습 전략 설계

- 어떤 전략으로 학습하기로 했는가?
- 그 전략을 선택한 이유는?
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

### 2. 전략 평가

- 효과적이었던 것과 그 이유
- 비효과적이었던 것과 그 이유

### 3. AI 피드백

- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
- 바꿀 것과 그 이유

---

## 학습 로그 #2

**시간**: MM/DD HH:MM ~ HH:MM (약 \_\_분)
**학습 범위**:

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

### 2. 전략 평가

- 효과적이었던 것과 그 이유
- 비효과적이었던 것과 그 이유

### 3. AI 피드백

- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
- 바꿀 것과 그 이유

---

...

- 효과적인 학습이 왜 중요한가?
- 나의 기존 학습법 v1 떠올려보기
- 공유 및 피드백
- 효과적으로 학습하는 사람들은 어떤 모습을 하고 있나요?
- 효과적인 학습자의 특징 정리
- 학습법 v2 Refactor
- 학습법 v2 활용하여 실제로 학습해보기
- 학습 회고
- 공유
- 학습법 v3 세우기
- 회고
- 강의 피드백
